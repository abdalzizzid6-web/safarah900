# ADMIN_REMEDIATION_MASTER_PLAN.md

## الهدف
تحسين بنية النظام، تقليل التكرار، تعزيز الأداء، وضمان استقرار لوحة التحكم والبيانات.

## المرحلة الأولى: الهيكلية (Architecture Consolidation)
- [ ] دمج الخدمات المكررة في `src/services` وتقليل عددها من 36+ إلى خدمات أساسية موحدة (مثل `MatchService`, `TeamService`, `NewsService`).
- [ ] توحيد المستودعات (Repositories) لتقليل التعقيد.
- [ ] حذف المكونات غير المستخدمة في `/src/admin`.

## المرحلة الثانية: الأداء (Performance & Firestore Optimization)
- [ ] فرض سياسة التخزين المؤقت (Cache) لجميع استعلامات API (60 ثانية للبيانات الحية، 24 ساعة للمنتهية).
- [ ] تحديث استعلامات Firestore لضمان تقليل القراءات (قاعدة الـ 20 قراءة).
- [ ] إزالة أي `onSnapshot` غير ضروري واستبداله بـ `getDoc`.

## المرحلة الثالثة: لوحة التحكم (Admin Panel Cleanup)
- [ ] إصلاح أي مكونات معطلة في `/src/admin`.
- [ ] تحسين أداء أدوات (Widgets) لوحة التحكم.

## المرحلة الرابعة: SEO والأمان (SEO & Security Hardening)
- [ ] التأكد من تطبيق Schema markup لجميع الصفحات.
- [ ] مراجعة قواعد `firestore.rules`.

## الموعد المستهدف
بدء التنفيذ فوراً وفقاً للأولوية.
